package in.ser.the_ultimate_scrum_simulator.model;

public enum CreateScenarioStatus {
    SUCCESS,
    INVALID_TITLE,
    INVALID_CREATOR,
    INVALID_DIFFICULTY,
    DATABASE_ERROR,
    UNKNOWN_ERROR
}
